# DevOps Universal Scanner - Python Dependencies
# Pure Python 3.13 Engine

# Core scanning tools - Always use latest versions
checkov                       # Multi-cloud security scanner (latest version)
cfn-lint                      # CloudFormation linter (latest version)
pycfmodel                     # CloudFormation model parsing (latest version)

# Cloud provider SDKs (for live pricing APIs) - Optional, for enhanced features
boto3                 # AWS SDK
# botocore is a boto3 dependency, no need to specify separately
# azure-mgmt-compute>=30.0.0    # Azure Compute management (temporarily disabled - large dependency)
# azure-mgmt-pricing>=1.0.0b1   # Azure Pricing (package not available on PyPI)
# google-cloud-billing>=1.12.0  # GCP Billing API (temporarily disabled - large dependency chain)

# IaC parsing and analysis
pyyaml              # YAML parsing
python-hcl2           # Terraform HCL2 parsing
jinja2              # Template rendering

# Security and CVE scanning
packaging                     # Version parsing for CVE checks (latest version)
requests             # HTTP requests for CVE APIs

# Data processing
tabulate              # Table formatting for reports
jsonschema           # JSON validation

# System utilities
psutil                # System and process utilities

# Static analysis (optional - for enhanced CVE/security scanning)
 
# Development tools (removed to reduce build time - install separately if needed)
# pytest>=8.0.0                 # Testing framework
# pytest-cov>=4.0.0             # Coverage reporting
# black>=24.0.0                 # Code formatting
# ruff>=0.3.0                   # Fast linting

# Security updates for known CVEs
setuptools          # CVE-2025-47273 fix
wheel               # Build dependency
